# Base URL of the deployed FastAPI backend, no trailing slash.
# Leave empty for local dev (Vite proxies /api to localhost:8000).
# In production (e.g. Vercel), set this to your Render backend URL:
VITE_API_URL=https://pars-fanavar-api.onrender.com
