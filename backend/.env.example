# Comma-separated list of allowed frontend origins for CORS
FRONTEND_ORIGIN=http://localhost:5173,http://localhost:4173

# Optional: set this to enable GET /api/consultations (admin listing).
# Requests must send header  X-Admin-Key: <value>
ADMIN_API_KEY=
